compose:
  files:
    - compose.yaml
  project_name: myapp

broker:
  base_url: https://pact-broker.example.com
  username: should-not-be-here
  password: neither-should-this

versioning:
  strategy: git_sha

environments:
  production:
    services:
      api:
        pacticipant: api
