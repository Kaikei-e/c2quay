compose:
  files:
    - compose.yaml
  project_name: c2quay-minimal

broker:
  base_url: http://127.0.0.1:1

versioning:
  strategy: manifest_file
  options:
    path: versions.json

environments:
  production:
    all_or_nothing: true
    services:
      api:
        pacticipant: api
      web:
        pacticipant: web
